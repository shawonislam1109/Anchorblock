import React from 'react';

const Dashboard = () => {
    return (
        <div>
            <h1>This is the dashboard section on the way fot the make sure right way come back </h1>
        </div>
    );
};

export default Dashboard;